{% extends 'base.html.twig' %}

{% block title %}{% trans %}Edit category{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}Edit category{% endtrans %}</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Update'|trans}) }}

    <a class="btn btn-primary" href="{{ path('category_index') }}"><i class="fas fa-arrow-left"></i> {% trans %}Back to list{% endtrans %}</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
